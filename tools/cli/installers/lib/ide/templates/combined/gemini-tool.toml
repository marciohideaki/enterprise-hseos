description = "Executes the {{name}} tool from the HSEOS Method."
prompt = """
Execute the HSEOS '{{name}}' tool.

TOOL INSTRUCTIONS:
1. LOAD the tool file from {project-root}/{{hseosFolderName}}/{{path}}
2. READ its entire contents
3. FOLLOW every instruction precisely as specified

TOOL FILE: {project-root}/{{hseosFolderName}}/{{path}}
"""
