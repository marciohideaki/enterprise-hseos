# Codex Profile

Runtime-specific artifacts for Codex.

This profile contains Codex variants generated from Claude assets,
with runtime tokens adjusted (`CLAUDE.md` -> `AGENTS.md`, `.claude` -> `.codex`, `Claude Code` -> `Codex CLI`).