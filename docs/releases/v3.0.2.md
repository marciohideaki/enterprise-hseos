# HSEOS v3.0.2 Release Evidence

## Classification and authorization

- **Risk:** Low.
- **Reason:** backward-compatible CLI installation lifecycle fixes; no schema,
  dependency, or public API removal.
- **Repository-owner acceptance:** explicitly authorized in the active delivery
  session on 2026-08-31.
- **Change reference:** [PR #134](https://github.com/marciohideaki/enterprise-hseos/pull/134).
- **Stable promotion:** remains blocked until the governing v3 consumer
  notification window and staging validation are complete.

## Artifact and staging

- The Git tag must be exactly `v3.0.2` and point at the reviewed release commit.
- The tag workflow builds `hseos-3.0.2.tgz` with `npm pack` and publishes it only
  as a GitHub prerelease asset.
- `SHA256SUMS` binds the package and CycloneDX SBOM with asset-relative names.
- CI installs the tarball in a clean directory and runs the public CLI smoke
  before creating the prerelease.
- Post-publication validation must download the exact published asset, verify
  its checksum, install it cleanly, and exercise the CLI lifecycle.

## Corrected staging findings

- Fresh headless installation creates `.hseos/config/hseos.config.yaml` before
  writing state and optional feature sections.
- Every `install-plan --json` surface emits plain JSON without terminal UI
  decoration.
- Consumer compilation includes `.agents/adapters`, so adapter discovery is
  complete outside the source repository.
- `agent-core doctor` skips adapter checks when none are selected and validates
  each selected adapter's required surfaces.
- Uninstall output explicitly distinguishes removable managed runtime data from
  preserved portable project governance.

## Validation

- focused regressions cover configuration bootstrap, JSON parsing, consumer
  adapter materialization, selected-adapter doctor behavior, and uninstall
  messaging;
- a real temporary project completed install, nine-check doctor, JSON catalog
  consumption, and uninstall preservation checks;
- `npm test` completed successfully under Node.js 24 after rebuilding the local
  native SQLite binding for the active ABI;
- the enforced governance quality gate completed with zero failures.

## Rollback

Keep the release marked as prerelease and reinstall the immutable v3.0.1 GitHub
asset. Do not rewrite either tag or its published assets.

```bash
gh release edit v3.0.2 --prerelease
```

## Required evidence before stable promotion

- protected CI and clean-environment smoke are green;
- `sha256sum -c SHA256SUMS` succeeds in the release download directory;
- a clean tarball installation passes install, doctor, JSON plan, and uninstall
  lifecycle checks;
- the governing v3 consumer-notification window has elapsed;
- the release flag change and rollback command are reviewed immediately before
  execution.
