# HSEOS v3.0.1 Release Evidence

## Classification and authorization

- **Risk:** High.
- **Reason:** corrected first public v3 distribution with breaking project-state, side-car access, workflow, and capability contracts.
- **Repository-owner acceptance:** explicitly authorized in the active delivery session on 2026-08-31.
- **Production promotion:** not authorized before the 24-hour consumer-notification window and staging validation complete.

## Artifact and staging

- The Git tag must be exactly `v3.0.1` and point at the reviewed release commit.
- The tag workflow builds `hseos-3.0.1.tgz` with `npm pack` and publishes it only as a GitHub prerelease asset.
- `SHA256SUMS` binds the downloadable package and CycloneDX SBOM with asset-relative filenames that verify directly in the download directory.
- The workflow installs the tarball in a clean directory and runs the public CLI smoke before creating the GitHub prerelease.
- Post-publication validation downloads the exact release asset, verifies its checksum, installs it in another clean directory, starts the loopback-only state UI side-car, and exercises the rendered UI with a headless browser.

## Corrected staging findings

- v3.0.0 retained an internal directory prefix in `SHA256SUMS`; v3.0.1 generates checksums from inside the asset directory.
- v3.0.0 returned a favicon 404 visible in the browser console; v3.0.1 provides a cacheable no-content response before authentication.
- v3.0.0 remains an immutable prerelease and must not be promoted.

## Consumer notification

`HideakiSolutions/platform-gitops` is the sole installer consumer in the approved G9 registry. The v3.0.1 release PR records a new notification timestamp. Stable promotion must occur no earlier than 24 hours after that timestamp.

## Dependency audit

- all production dependencies are pinned exactly in `package.json` and the lockfile is committed;
- `npm audit --omit=dev` and the complete dependency-tree audit report zero known vulnerabilities;
- the deprecated `prebuild-install` package is transitive through `better-sqlite3`; the first driver release that removes it requires Node 22, so maintainers must remove it when the runtime contract can be upgraded without dropping supported Node 20 consumers;
- the release includes a CycloneDX SBOM covered by `SHA256SUMS`;
- the release tarball is regenerated after the version change and must pass the clean-install smoke again.

## Promotion

After staging and browser validation pass, and after the notification window elapses:

```bash
gh release edit v3.0.1 --prerelease=false
```

Promotion must verify the release asset checksum again before changing the prerelease flag.

## Rollback

If staging validation fails, keep the release marked as prerelease and record the failure. If validation fails after promotion, return the release to prerelease and retain the immutable tag and evidence for audit:

```bash
gh release edit v3.0.1 --prerelease
```

Consumers must remain pinned to the previous stable repository release until a corrected version is published. Git tags and downloaded artifacts are immutable evidence and are never rewritten.

## Required evidence before production

- protected CI and clean-environment smoke are green;
- `sha256sum -c SHA256SUMS` succeeds directly in the release download directory;
- a clean tarball installation runs `hseos --help`;
- the state UI renders successfully in a browser and its health/state/event requests succeed;
- no browser console error or failed same-origin request is observed;
- the 24-hour consumer-notification window has elapsed;
- the GitHub stable-promotion and rollback commands are reviewed immediately before execution.
