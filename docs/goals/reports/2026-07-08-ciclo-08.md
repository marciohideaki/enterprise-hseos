# Relatório do Ciclo 08 — Evolução Contínua e Controlada

> **Goal:** `docs/goals/GOAL - Evolucao continua e controlada.txt` · **Data:** 2026-07-09
> **Branch:** `feature/capability-packaging-v1` · **Objetivo do ciclo** (definido no ciclo 07): plano de conformidade MCP pós-GA.

---

## 1. O que o ciclo entregou

### 1.1 protocolVersion com fonte única

`tools/lib/mcp-protocol.js` agora é a única declaração da revisão MCP (`2024-11-05`); o transporte compartilhado e o transporte próprio do axon-bridge importam de lá. O bump para a GA 2026-07-28 vira mudança de uma linha (antes: 2 hardcodes independentes que podiam divergir silenciosamente).

### 1.2 Contrato entre os 4 servers, testado em runtime

`test/test-mcp-contract.js` (novo, no `npm test`): **21 asserções** provando em runtime — 3 servers sondados via stdio real e o axon-bridge via HTTP real em porta efêmera — que todos reportam a MESMA `protocolVersion` (a do módulo central), retornam `serverInfo`, e expõem tools únicos com `name`/`description`/`inputSchema`. Divergência futura entre servers = CI vermelho.

### 1.3 RFC de adoção pós-GA (decisão preparada)

`docs/rfc/2026-07-09-mcp-post-ga-conformance.md`: opções A (SDK oficial), B (subset manual + contrato, **recomendada**) e C (híbrido — SDK para o primeiro server que precisar de resources/prompts/Tasks), com plano concreto: em até 30 dias da GA, implementar no transporte só o que afeta o subset (negociação de versão, cache metadata, headers HTTP) e bumpar a constante uma vez. Promover a ADR é decisão humana (numeração: 0017 está reservado pela branch `task/stacked-branch-policy`).

## 2. Estado dos gates ao final

`npm test` completo verde (agora 21+ suítes, incl. contrato MCP) · `verify` 88/88 · format/lint verdes.

## 3. Fila restante

1. Espelho PT-BR de `docs/capabilities.md`.
2. `update`/`updateCore` consumindo `files-manifest.csv` como `recordedHash` (3 pontos completos).
3. Decisões humanas: RFC MCP → ADR; plugins-emit (ADR-0009); `enforce_admins: true`; merge `task/stacked-branch-policy`; replay-mode falso-positivo; `standards-adoption-metrics`.

## 4. Próximo objetivo de maior impacto (Ciclo 09)

Com as 8 frentes estruturais do backlog original fechadas (integridade, hooks, enforcement, manifest, audit, idempotência, capabilities, MCP), o maior impacto muda de correção para **consolidação de entrega**: preparar o fechamento do branch — atualizar a especificação PT-BR §12 com o estado real pós-ciclos (vários itens da lista original já não refletem o working tree), gerar o sumário executivo do PR (8 ciclos, ~50 testes novos, mudanças de comportamento documentadas) e deixar o branch pronto para o commit/PR humano via `hseos pr closeout`.

---

*Ciclo 08 executado em sequência aos ciclos 01-07. Working tree acumulado aguarda commit humano.*
