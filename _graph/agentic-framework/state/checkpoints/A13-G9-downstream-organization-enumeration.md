# A13 / G9 Downstream Organization Enumeration Checkpoint

**Artifact type:** Governed goal checkpoint

**Scope:** A13 dependency evidence for the complete downstream compatibility perimeter

**Status:** GitHub perimeter enumerated; release-bound activation evidence remains incomplete

Authenticated read-only enumeration corrected the G9 candidate set from four to five repositories:
the complete `HideakiSolutions` GitHub organization scan found `cambio-real` in addition to
`events-platform`, `intent-os` and `platform-gitops`; the known personal scope adds `ai-agents-os`.
No default branch in either enumerated scope contains the bounded plugin-v1 evidence path.

The full evidence, exact commits and claim classifications are recorded in
`../../../harness-unification/state/checkpoints/G9-downstream-organization-enumeration.md`. The SSH
alias used by the local `platform-gitops` checkout resolves to the canonical GitHub identity, but a
future collector binding must use an isolated checkout with an explicit network URL. The corrected
registry remains `pending`: owner-approved perimeter and membership, a published release containing
the registry, real R/R+1 migration evidence, 30/30 days and the final human gate are still required.
